{
  "name": "wxve-chat-cdk",
  "version": "0.1.0",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "cd .. && trunk build --release && cd cdk && cdk deploy",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "@types/node": "^20",
    "aws-cdk": "^2",
    "typescript": "~5.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2",
    "constructs": "^10"
  }
}
